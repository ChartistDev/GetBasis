import {basisLogo} from "../../images/basis";
const initialState = {
    heading : {
        imageUrl: basisLogo
    }
}
const Heading = (state = initialState, action) => {
    return state;
}
export default Heading;