import {basisLogo} from "../../images/basis";
const initialState = {
    heading : {
        imageUrl: basisLogo
    }
}
const Login = (state = initialState, action) => {
    return state;
}
export default Login;